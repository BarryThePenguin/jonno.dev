export const metadata = {
	title: '👋🏻',
};

export default function Layout({children}) {
	return (
		<div className="min-h-screen bg-home -m-4 sm:-m-8 py-20 sm:py-24">
			<main className="max-w-none prose prose-2xl prose-headings:bg-white prose-headings:p-8 prose-a:text-gray-500 prose-a:transition-colors prose-a:hover:text-blue-400">
				{children}
			</main>
		</div>
	);
}

# Jonathan Haines

## Frontend Developer

## [See what I do](/resume)

<a
	rel="me"
	href="https://mastodon.social/@BarryThePenguin"
	title="Mastodon"
	className="hidden"
	style={{visibility: 'hidden'}}
></a>
